# 🔧 Troubleshooting: Кнопки не активні

**Проблема:** Кнопки на homepage не кликаються або не ведуть на сторінки.

---

## ✅ Швидкі рішення

### 1. Hard Refresh (Найчастіше допомагає)

**Windows:**

```
Ctrl + Shift + R
```

або

```
Ctrl + F5
```

**Причина:** Браузер показує старий кешований вигляд сайту.

---

### 2. Перевірка Vercel Deployment

**Відкрий термінал:**

```powershell
# Подивитися статус останнього deployment
npx vercel ls --yes
```

**Очікуваний результат:**

```
✅ Production: Ready
```

**Якщо "Building...":**

- Зачекай 2-3 хвилини
- Потім зроби hard refresh

---

### 3. Перевірка сторінок

**Спробуй відкрити напряму:**

```
https://yana-diia-v3.vercel.app/lego
https://yana-diia-v3.vercel.app/pipeline
https://yana-diia-v3.vercel.app/evaluation
```

**Якщо 404:**

- Deployment ще не завершився
- АБО є помилка в build

---

### 4. Локальна перевірка

**Запусти локально:**

```powershell
cd c:\igor\Projects\ДІЯ\01_Projects\Yana.Diia_v3
npm run dev
```

**Відкрий:**

```
http://localhost:3000
```

**Натисни кнопки:**

- Якщо працюють локально → проблема з Vercel
- Якщо не працюють локально → проблема в коді

---

## 🎯 Швидкий тест

**1. Відкрий DevTools:**

```
F12
```

**2. Перейди на вкладку Console**

**3. Клікни по кнопці**

**4. Подивись чи є помилки**

---

## 🔍 Діагностика

### Кнопка сіра/неактивна (disabled)

**Причина:** CSS проблема або JavaScript error

**Рішення:**

```
Hard refresh (Ctrl + Shift + R)
```

### Кнопка активна але не веде нікуди

**Причина:** Routing проблема

**Перевірка:**

```javascript
// В DevTools Console
document.querySelector('a[href="/lego"]')
// Має показати Link елемент
```

### Кнопка веде на 404

**Причина:** Сторінка не задеплоїлась

**Рішення:**

```powershell
# Redeploy
npx vercel --prod --yes
```

---

## ✅ Фінальний checklist

- [ ] Hard refresh в браузері
- [ ] Vercel deployment = Ready
- [ ] Сторінки відкриваються напряму
- [ ] DevTools Console без помилок
- [ ] Локально все працює

---

## 🚀 Якщо нічого не допомагає

**1. Повний redeploy:**

```powershell
npx vercel --prod --force --yes
```

**2. Очистити Vercel cache:**

```
Project Settings → General → Redeploy with Cache Cleared
```

**3. Перевірити vercel.json:**

```json
{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
```

---

**99% випадків:**  
Проблема вирішується **Hard Refresh** 🔄
