# 🚨 КРИТИЧНО: Видалення .gemini та .kiro з GitHub

## Проблема
Папки `.gemini/` та `.kiro/` містять внутрішні артефакти AI-асистентів (Kiro/Gemini) і НЕ повинні бути на GitHub.

## Статус .gitignore
✅ `.gitignore` вже налаштований правильно:
```
# Agent Artifacts (Kiro/Gemini AI assistants)
.gemini/
.kiro/
```

## ⚠️ ВАЖЛИВО
Якщо ці папки вже були закомічені раніше, `.gitignore` їх НЕ видалить з репозиторію автоматично!

---

## 🔧 Рішення: Видалити з Git (але залишити локально)

### Варіант 1: Видалити тільки з Git (рекомендовано)
```bash
# Перейти в папку проекту
cd "C:\igor\Projects\ДІЯ\01_Projects\Yana.Diia_v3"

# Видалити з git tracking (але залишити локально)
git rm -r --cached .gemini
git rm -r --cached .kiro

# Закомітити зміни
git commit -m "chore: remove .gemini and .kiro from git tracking"

# Запушити на GitHub
git push origin main
```

### Варіант 2: Перевірити чи папки в git
```bash
# Перевірити чи файли tracked
git ls-files .gemini/
git ls-files .kiro/

# Якщо команда повертає файли - вони в git і треба їх видалити
```

### Варіант 3: Повне очищення з історії (якщо дуже критично)
⚠️ **УВАГА**: Це перепише всю історію git!

```bash
# Використати git filter-branch (складно, не рекомендовано)
git filter-branch --force --index-filter \
  "git rm -r --cached --ignore-unmatch .gemini .kiro" \
  --prune-empty --tag-name-filter cat -- --all

# Або використати BFG Repo-Cleaner (простіше)
# Завантажити: https://rtyley.github.io/bfg-repo-cleaner/
java -jar bfg.jar --delete-folders .gemini --delete-folders .kiro
git reflog expire --expire=now --all
git gc --prune=now --aggressive
git push origin --force --all
```

---

## ✅ Перевірка після видалення

### 1. Локально перевірити .gitignore
```bash
git status
# Не повинно показувати .gemini/ та .kiro/
```

### 2. Перевірити на GitHub
Відкрити: https://github.com/010io/Yana.Diia_v3
- Переконатися що папок `.gemini/` та `.kiro/` немає в репозиторії

### 3. Перевірити що локально папки залишились
```bash
dir .gemini
dir .kiro
# Повинні показати файли (локально вони потрібні для роботи)
```

---

## 📋 Що робити ЗАРАЗ (покрокова інструкція)

1. **Відкрити Git Bash або PowerShell з git**
   ```bash
   cd "C:\igor\Projects\ДІЯ\01_Projects\Yana.Diia_v3"
   ```

2. **Перевірити чи папки в git**
   ```bash
   git ls-files .gemini/ | wc -l
   git ls-files .kiro/ | wc -l
   ```
   - Якщо `0` - все ок, папки не в git
   - Якщо `> 0` - треба видаляти

3. **Видалити з git tracking**
   ```bash
   git rm -r --cached .gemini
   git rm -r --cached .kiro
   git commit -m "chore: remove AI artifacts from git tracking"
   git push origin main
   ```

4. **Перевірити на GitHub**
   - Зайти на https://github.com/010io/Yana.Diia_v3
   - Переконатися що папок немає

---

## 🔒 Додаткові налаштування безпеки

### Додати до .gitattributes (опціонально)
Створити файл `.gitattributes`:
```
.gemini/** export-ignore
.kiro/** export-ignore
```

### Перевірити інші файли
Також перевірити чи немає інших чутливих файлів:
```bash
git ls-files | grep -E "\.(env|key|secret|credentials)"
```

---

## 📞 Якщо щось пішло не так

1. **Папки зникли локально**
   - Вони були видалені з диску
   - Відновити з backup або Kiro створить знову

2. **Помилка при push**
   - Можливо конфлікт з remote
   - Спробувати: `git pull --rebase` потім `git push`

3. **Папки все ще на GitHub**
   - Почекати 5-10 хвилин (кеш GitHub)
   - Оновити сторінку з Ctrl+F5
   - Перевірити інші гілки: `git branch -a`

---

## ✅ Фінальний чеклист

- [ ] `.gitignore` містить `.gemini/` та `.kiro/`
- [ ] Виконано `git rm -r --cached .gemini .kiro`
- [ ] Закомічено та запушено зміни
- [ ] Перевірено на GitHub - папок немає
- [ ] Локально папки залишились (для роботи Kiro)
- [ ] Команда знає що ці папки не треба комітити

---

**Створено**: 2025-11-27  
**Статус**: 🚨 КРИТИЧНО - виконати негайно перед demo day
